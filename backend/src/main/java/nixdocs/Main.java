package nixdocs;

import org.eclipse.jetty.server.Server;
import org.eclipse.jetty.servlet.ServletContextHandler;
import org.eclipse.jetty.servlet.ServletHolder;

public class Main {
    public static void main(String[] args) throws Exception{
        // var server = new Server(Integer.parseInt(System.getProperty("API_PORT","8080")));
        // var context = new ServletContextHandler(ServletContextHandler.No_SESSION);
        // context.addServlet(new ServletHolder(null), "/");
        // server.setHalder(context);
        // server.setStopAtShutdown(true);
        // server.start();
        // server.join();
    }
    
}
